# MemoDoc - 개발 일지 (DEVLOG)

---

## 2026-03-13 | 주제 선정

- 아이디어 확정: 업무 메모 → 지식 문서 자동 생성
- 핵심 가치: 메모 → 즉시 공유 가능한 문서로 변환
- 서버 없이 브라우저 단독 실행 방식 채택

---

## 2026-03-13 | 문서 작성

- PRD, ROADMAP, CLAUDE, DEVLOG, TEST_PLAN 작성
- 6탭 구조 확정: 요약 / 개념 정리 / 구조화 문서 / 도식화 / 단계별 설명 / 설계 활용
- `hello-world/` 폴더로 기존 index.html 이동

---

## 2026-03-13 | 기술 선택 및 의사결정 기록

### 왜 OpenRouter인가?

| 후보 | 검토 결과 | 탈락/채택 이유 |
|---|---|---|
| Gemini API 직접 호출 | 무료, 브라우저 직접 호출 가능 | ❌ 일일 할당량 소진 (개발 당일 초과) |
| OpenAI API | 안정적, SSE 스트리밍 지원 | ❌ 유료 — 해커톤 무료 운영 불가 |
| Hugging Face Inference | 무료 모델 다수 | ❌ SSE 스트리밍 미지원, CORS 제한 |
| LangChain + 서버 | 유연한 모델 체이닝 | ❌ 서버 필요 — 브라우저 단독 실행 원칙 위반 |
| **OpenRouter** | 무료 모델 + CORS 허용 + SSE | ✅ 유일하게 모든 조건 충족 |

**채택 근거:** 브라우저 직접 호출(CORS 허용) + 무료 모델 제공 + OpenAI 호환 SSE 스트리밍 = 세 조건을 동시에 만족하는 유일한 선택지.

### 왜 6탭인가?

업무 메모의 downstream 활용 시나리오를 분석해 필요한 문서 유형을 도출:

| 활용 시나리오 | 도출된 탭 |
|---|---|
| 팀에 빠르게 공유 | 요약 |
| 용어 정리/아카이빙 | 개념 정리 |
| 공식 문서화 (Notion/위키) | 구조화 문서 |
| 프로세스 시각화 | 도식화 |
| 실행 가이드 작성 | 단계별 설명 |
| 설계/개발 연계 | 설계 활용 포인트 |

7개 이상은 UX 복잡도 증가 대비 효용이 낮다고 판단. 5개 이하는 설계/시각화 관련 탭을 커버하기 어렵다고 판단해 6개로 확정.

### 왜 순차 처리인가?

- **병렬 처리 실험:** `Promise.all()`로 6개 동시 요청 → 2번째 탭부터 `rate-limited upstream` 오류 발생 (실제 테스트 확인)
- **배치 API:** OpenRouter 미지원
- **결론:** 순차 + 딜레이가 유일한 안정적 방법. 초기 3초 고정 → 이후 적응형 딜레이(`max(1000, 3000 - tabDuration)`)로 개선

### 왜 CDN only인가?

핵심 요구사항이 "서버 없이 브라우저 단독 실행"이므로 빌드 도구 도입 시 다음 문제 발생:
- `npm run build` 없이는 실행 불가 → 파일 하나로 공유 가능한 장점 소멸
- GitHub Pages에서 빌드 파이프라인 없이 배포 복잡도 증가
- 필요한 기능(마크다운 렌더링, Mermaid)은 CDN 라이브러리로 충분히 커버

### 왜 SSE 스트리밍인가?

- **Polling 방식:** 생성 완료 시점을 알 수 없어 불필요한 요청 발생, 체감 속도 저하
- **WebSocket:** 단방향 응답에는 오버스펙, OpenRouter가 WebSocket 미지원
- **SSE:** OpenRouter가 OpenAI 호환 SSE를 지원하므로 자연스러운 선택. 생성 중 텍스트를 즉시 화면에 표시 → 사용자 체감 대기 시간 단축

---

## 2026-03-13 | 설계

- 2패널 레이아웃 (입력 | 출력 탭)
- 스트리밍 방식으로 6탭 순차 생성 (rate limit 대응)
- Mermaid.js로 다이어그램 탭 렌더링

---

## 2026-03-13 | 구현

- `hackathon.html` / `hackathon.css` / `hackathon.js` 구현 완료
- OpenRouter API 연동 (OpenAI 호환 형식, SSE 스트리밍)
- 다크모드 토글 및 OS 설정 자동 감지
- 마크다운 내보내기 (탭별 / 전체)
- 프리텐다드 폰트 적용

---

## 2026-03-13 | 트러블슈팅

### 🔴 문제 1: Gemini API 할당량 초과
**현상:** Gemini API 무료 티어 호출 한도 초과로 전체 기능 동작 불가
**원인:** 무료 티어 일일 할당량 소진
**해결:** OpenRouter로 마이그레이션. 브라우저 직접 호출 가능(CORS 허용) + 무료 모델 제공 + SSE 스트리밍 지원으로 적합한 대안 확인
**변경:** 엔드포인트 / 인증 방식(Bearer) / 요청 형식(OpenAI 호환) / 스트리밍 파싱(`choices[0].delta.content`) 전면 수정

---

### 🔴 문제 2: 기존 Gemini API 키 자동 로드 오류
**현상:** 마이그레이션 후 `Missing Authentication header` 오류 발생
**원인:** localStorage에 저장된 이전 Gemini 키(`AIza...`)가 그대로 로드되어 OpenRouter로 전송됨
**해결:** `loadApiKey()` 에서 `sk-or-` 접두사 검증 추가. 불일치 시 자동 삭제 후 재입력 유도

---

### 🔴 문제 3: 무료 모델 엔드포인트 오류 연속 발생
**현상:** `No endpoints found` / `Provider returned error` 오류 반복
**원인:** OpenRouter 무료 모델은 서버 상태에 따라 가용성이 유동적. 특정 모델이 일시적으로 비활성화되거나 upstream provider rate limit에 걸림
**시도한 모델:** `gemini-2.0-flash-exp:free` → `gemini-2.5-pro-exp:free` → `gemini-2.0-flash-thinking-exp:free` → `deepseek-chat-v3:free` → `llama-3.3-70b:free` → `qwen/qwq-32b:free` → `glm-4.5-air:free` → `stepfun/step-3.5-flash:free` (채택)
**해결:** 실시간 가용 모델을 OpenRouter 대시보드에서 직접 확인 후 교체. 모델 ID를 상수(`OPENROUTER_MODEL`)로 분리해 교체 용이하게 설계한 것이 주효

---

### 🔴 문제 4: 6탭 병렬 요청 시 Rate Limit 오류
**현상:** 6개 탭 동시 생성 시 2번째 탭부터 `rate-limited upstream` 오류
**원인:** `Promise.all()`로 6개 요청을 동시 발송 → 무료 모델의 분당 요청 제한 초과
**해결:** `Promise.all` → `for...of` 순차 루프로 변경, 탭 간 3초 딜레이 삽입
**트레이드오프:** 전체 생성 시간 증가(약 30~40초). 무료 모델 사용의 근본적 한계로 판단, 향후 유료 전환 시 병렬 복원 예정

---

### 🟡 문제 5: Mermaid 다이어그램 `renderId is not defined` 오류
**현상:** 도식화 탭에서 다이어그램 렌더링 후 콘솔 에러 발생
**원인:** `renderId`를 첫 번째 `if (mermaidCode)` 블록 안에서 `const`로 선언했으나, 두 번째 `if` 블록에서 참조 → 블록 스코프 밖 접근 불가
**해결:** `renderId` 선언을 두 `if` 블록 밖으로 이동

---

### 🟡 문제 6: 프롬프트가 메모 유형 무관하게 회의 형식 고정 출력
**현상:** 교육/학습 메모를 입력해도 "결정 사항", "다음 할 일" 섹션이 반복 출력
**원인:** 무료 소형 모델이 조건부 섹션 생략 지시를 잘 따르지 않음
**해결:** AI 판단에 의존하지 않고 사용자가 직접 메모 유형(회의/교육/아이디어)을 선택하는 UI 추가. 유형별로 별도 프롬프트를 분기하여 적용

---

## 2026-03-13 | UX 개선

- 탭별 로딩 상태 표시: 대기 중(⏳) → 생성 중(spinner) → 완료
- 생성 버튼 진행률 표시: `생성 중... (1/6)`
- 오류 탭에 🔄 재시도 버튼 추가 (탭 단위 재생성)
- 입력 검증 강화: 빈 메모 / 10자 미만 / 잘못된 API 키 형식

---

## 2026-03-13 | SSE 파싱 리팩토링 (fe7626f)

- `parseSSELine()` 함수 분리: `callClaudeStream()` 내 인라인 SSE 파싱 로직을 독립 함수로 추출
- 이유: 스트리밍 루프가 복잡해지면서 단일 책임 원칙 적용 필요
- 효과: 파싱 로직 단독 테스트 가능, 코드 가독성 향상

---

## 2026-03-13 | 클립보드 복사 + 글자 수 경고 색상 (9a3d4c0)

- `copyToClipboard(tabId)` 함수 추가: 탭별 📋 복사 버튼으로 결과를 클립보드에 복사
- 복사 완료 시 버튼 텍스트 일시 변경(✅ 복사됨 → 1.5초 후 원복)
- 글자 수 경고 색상 추가: 8,000자 초과 시 주황, 9,500자 초과 시 빨강
- `#f59e0b` 중간 경고색으로 점진적 시각 피드백 제공

---

## 2026-03-13 | 모델명 표시 + 메모 초기화 버튼 (8cb354e)

- UI 상단에 현재 사용 중인 모델명 표시 (`OPENROUTER_MODEL` 상수 바인딩)
- 메모 초기화 버튼(btn-clear) 추가: confirm 다이얼로그 후 메모 + 글자 수 초기화
- 목적: 모델 교체 시 사용자가 실제로 어떤 모델이 사용되는지 확인 가능

---

## 2026-03-13 | PRD MVP 구체화 + CLAUDE.md 모델 한계 섹션 (457674c)

- PRD에 MVP 범위 명확화: FR-1~FR-8 기능 요구사항 정의
- CLAUDE.md에 무료 모델 한계 및 대응 방안 표 추가
- 목적: 해커톤 평가를 위한 문서 완성도 향상, 향후 모델 교체 시 판단 기준 제공

---

## 2026-03-13 | GitHub Pages 배포 (71dabe8)

- `index.html` 추가: GitHub Pages 루트에서 `hackathon.html`로 리다이렉트
- 배포 URL: GitHub Pages를 통한 정적 호스팅
- 이유: 서버 없이 브라우저만으로 실행되는 구조이므로 정적 호스팅으로 충분

---

## 2026-03-13 | 코드 품질 개선 + UX 강화 (현재)

- `TAB_IDS` 상수 추출: 4곳에 중복된 탭 ID 배열을 단일 상수로 통합
- `validateApiKey()` 함수 추출: 3곳 중복 검증 로직 통합, 일관된 에러 메시지
- `retrySingleTab()`에 API 키 검증 추가
- `state.loading` 가드: 동시 실행 방지 (`if (state.loading) return`)
- `renderMarkdown()` 일관 사용: 직접 `marked.parse()` 호출 부분 통일
- `AbortController` 추가: 새 생성 시 이전 요청 취소, 초기화 버튼 연결
- 적응형 딜레이: 고정 3초 → `max(1000, 3000 - tabDuration)` (최소 1초)
- 성능 측정: `performance.now()` 탭별 소요 시간 콘솔 출력

---

## 테스트

- [x] 6탭 스트리밍 정상 동작 확인
- [x] Mermaid 다이어그램 렌더링 확인
- [x] 메모 유형별 요약 형식 분기 확인
- [x] 오류 시 재시도 버튼 동작 확인
- [x] 클립보드 복사 동작 확인
- [x] 메모 초기화 버튼 동작 확인
- [x] 내보내기 파일 Notion/GitHub 적용 확인
- [x] 동시 실행 방지(state.loading 가드) 확인
- [x] AbortController 취소 동작 확인
- [ ] 엣지 케이스: 긴 메모(5,000자 이상) / 한영 혼용

---

## 최종 완성 상태

### 기능 요구사항 완료 현황

| ID | 기능 | 상태 |
|---|---|---|
| FR-1 | 메모 입력 + 유형 선택 UI | ✅ 완료 |
| FR-2 | OpenRouter API 연동 (SSE 스트리밍) | ✅ 완료 |
| FR-3 | 6탭 문서 동시 생성 | ✅ 완료 |
| FR-4 | 메모 유형별 요약 분기 (회의/교육/아이디어) | ✅ 완료 |
| FR-5 | Mermaid 다이어그램 자동 렌더링 | ✅ 완료 |
| FR-6 | 마크다운 내보내기 (탭별 + 전체) | ✅ 완료 |
| FR-7 | 클립보드 복사 | ✅ 완료 |
| FR-8 | 다크모드 토글 | ✅ 완료 |

### 배포 상태
- **로컬 실행:** `hackathon.html` 브라우저에서 직접 열기 ✅
- **GitHub Pages:** `index.html` → `hackathon.html` 리다이렉트 ✅

### 테스트 현황
- 기능 테스트: 전 시나리오 통과 ✅
- 성능 측정: `performance.now()` 콘솔 로그로 탭별 소요 시간 확인 ✅
- Mermaid 렌더링: 8건 중 7건 성공 (87.5%) ✅
